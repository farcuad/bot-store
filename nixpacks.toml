[variables]
    NIXPACKS_NODE_VERSION = '22.12.0'

[install]
    # Forzar la instalación de dependencias de desarrollo (vite, tsc, etc.)
    cmds = ['npm ci --include=dev']

[build]
    # Compilar el proyecto
    cmds = ['npm run build']
